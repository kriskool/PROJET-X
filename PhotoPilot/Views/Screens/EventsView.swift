import SwiftUI

struct EventsView: View {
    var body: some View {
        BaseScreenView(title: "Events", subtitle: "Organize memories by event.", symbolName: "calendar")
    }
}

#Preview {
    EventsView()
}
