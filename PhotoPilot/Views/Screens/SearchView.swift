import SwiftUI

struct SearchView: View {
    var body: some View {
        BaseScreenView(title: "Search", subtitle: "Find photos across the library.", symbolName: "magnifyingglass")
    }
}

#Preview {
    SearchView()
}
