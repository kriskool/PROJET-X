import SwiftUI

struct DashboardView: View {
    var body: some View {
        BaseScreenView(title: "Dashboard", subtitle: "Overview for PhotoPilot.", symbolName: "house")
    }
}

#Preview {
    DashboardView()
}
