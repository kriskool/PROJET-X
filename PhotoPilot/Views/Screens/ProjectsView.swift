import SwiftUI

struct ProjectsView: View {
    var body: some View {
        BaseScreenView(title: "Projects", subtitle: "Build and manage photo projects.", symbolName: "folder")
    }
}

#Preview {
    ProjectsView()
}
