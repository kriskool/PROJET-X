import SwiftUI

struct PlacesView: View {
    var body: some View {
        BaseScreenView(title: "Places", subtitle: "Browse photos by location.", symbolName: "mappin.and.ellipse")
    }
}

#Preview {
    PlacesView()
}
