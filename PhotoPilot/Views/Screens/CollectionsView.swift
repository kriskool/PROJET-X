import SwiftUI

struct CollectionsView: View {
    var body: some View {
        BaseScreenView(title: "Collections", subtitle: "Group photos into useful collections.", symbolName: "tag")
    }
}

#Preview {
    CollectionsView()
}
