import SwiftUI

struct SettingsView: View {
    var body: some View {
        BaseScreenView(title: "Settings", subtitle: "Configure PhotoPilot preferences.", symbolName: "gearshape")
    }
}

#Preview {
    SettingsView()
}
